export interface User {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
  role: string;
  phoneNumber: string;
  firstLogin: string;
  // add more fields if your user has more information
}
