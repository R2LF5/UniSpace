import { AppCapitalizeFirstDirective } from './app-capitalize-first.directive';

describe('AppCapitalizeFirstDirective', () => {
  it('should create an instance', () => {
    const directive = new AppCapitalizeFirstDirective();
    expect(directive).toBeTruthy();
  });
});
